import React from 'react'
const User = (user) => {
    console.log(user)
    // const blogList = user.user.blogs
    // console.log(blogList)
    return(
        <div>
        {/* <div>
        <i>{user.user.name} wrote </i>
        <ul>
            {blogList.map(blog => <li>{blog.title}</li>)}
        </ul>
        <i>blogs.</i>
      </div> */}

        </div>
    )
}

export default User

